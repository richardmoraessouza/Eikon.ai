"use client";

import Layout from "../../src/components/navigation/Layout/Layout";

export default function LayoutComMenu({ children }: { children: React.ReactNode }) {
  return <Layout>{children}</Layout>;
}
